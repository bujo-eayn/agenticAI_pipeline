# graph/nodes/user_input.py
def user_input_node(state):
    return state
# This node simply returns the current state as it is.
# It serves as the entry point for the graph, allowing the user to provide initial input.